import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'DigiNeom - Software Development & Web Design Agency',
  description: 'Big ideas, smart strategies, and endless creativity to supercharge your brand! We create stunning digital experiences through innovative software development and cutting-edge web design.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

